# Netlify configuration for Next.js 16 App Router
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_USE_NETLIFY_EDGE = "true"

[[redirects]]
  from = "/api/*"
  to = "https://elite-concierge-api-557456081985.europe-west2.run.app/api/:splat"
  status = 200

# Continuous Inference WebSocket Proxy (Optional, direct connection preferred)
# [[redirects]]
#   from = "/ws/*"
#   to = "wss://elite-concierge-api-zx7q.a.run.app/ws/:splat"
#   status = 200
