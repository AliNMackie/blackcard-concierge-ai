import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/lib/auth-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elite Concierge AI | Intelligent Fitness & Wellness",
  description: "AI-powered fitness concierge delivering personalised wellness intelligence, real-time biometrics, and expert oversight for peak performance.",
  metadataBase: new URL("https://blackcard-concierge.netlify.app"),
  openGraph: {
    title: "Elite Concierge AI",
    description: "Personalised wellness intelligence for discerning individuals.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
